#!/usr/bin/env ruby
# -*- mode: Ruby -*-

require 'optparse'
require 'ostruct'
require 'pp'

options = OpenStruct.new
OptionParser.new do |opts| 
  opts.on("-l", "--list") { |v| options.list_mode = v }
end

if options.list_mode then


end
